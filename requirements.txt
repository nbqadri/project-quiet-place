# YouTube Shorts Generator – Python dependencies
# Install with: pip install -r requirements.txt

anthropic>=0.25.0          # Anthropic SDK (used for reference; we call REST directly)
requests>=2.31.0            # HTTP client for Pexels & Claude REST API
python-dotenv>=1.0.0        # .env file loading
